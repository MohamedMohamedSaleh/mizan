enum AccountType {
  asset,
  liability,
  equity,
  revenue,
  expense,
}

enum PaymentAccountType {
  cash,
  bank,
}

enum ExpenseStatus {
  draft,
  saved,
  voided,
}

enum RecurrenceType {
  daily,
  weekly,
  monthly,
  yearly,
}

enum JournalSourceType {
  manual,
  expense,
}

enum JournalEntryStatus {
  draft,
  posted,
  voided,
}
